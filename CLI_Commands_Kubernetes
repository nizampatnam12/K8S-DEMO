CLI commandsfor kubernetes 

k9s
kubectl get pods -A
kubectl apply -f ./deployment.yaml
kubectl delete -f ./deployment.yaml
kubectl apply -f ./deployment.yaml
kubectl get pods -A
./mvnw spring-boot:build-image  
docker images  
docker tag demo:0.0.1-SNAPSHOT nizampatnamvamshi/demo:0.0.1-SNAPSHOT
docker login    
eval $(minikube docker-env)
kubectl get nodes -o wide  
docker run demo:0.0.1-SNAPSHOT   
minikube start --cni=calico  
ubectl run --rm --stdin --image=hello-world --restart=Never --request-timeout=30 test-pod
minikube dashboard  
minikube node add
echo mongopassword | base64
echo mongouser | base64      
git config --global user.name " "

git config --global user.email “@gmail.com”
git config -l --show-origin
https://start.spring.io

AI BOOKS:
1. Build a large language model from scratch by Sebastian Raschka.
2. Building LLMs for production by Louie Peters.
3. RAG-Driven Generative AI by Denis Rothman
4. AL Engineering by Chip Huyen

Java Starting Places
1. https://openjdk.org
2. https://dev.java
3. https://inside.java
4. https://www.youtube.com/java

Linux commands:
ollama list
free -h
ollama run llama2:7b --verbose
df -h

sdk list java
sdk install java 8.0.442-amzn
sdk install 17.0.14-amzn
sdk install java 24-amzn  

sdk use java 8.0.442-amzn 
sdk use java 24-amzn  
sdk use java 17.0.14-amzn (default in my raspberry pi 5)
